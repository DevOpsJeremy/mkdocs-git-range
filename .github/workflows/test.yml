on:
  push:
    branches: ["*"]
jobs:
  checkout:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v4
        with:
          repository: DevOpsJeremy/DevOpsJeremy.github.io
      - name: List
        run: |
          pwd
          ls -a